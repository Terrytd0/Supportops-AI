name: CI

# TODO: this is an empty scaffold workflow. Flesh out lint/test steps below
# once dependencies and test suites are in place. Consider splitting into
# separate lint.yml / test.yml workflows if they diverge significantly.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # TODO: install dependencies (e.g. `pip install -e ".[dev]"`)
      # TODO: run `ruff check .`
      # TODO: run `mypy backend`
      - name: Placeholder
        run: echo "TODO -- add lint steps"

  test:
    runs-on: ubuntu-latest
    needs: lint
    # TODO: add postgres/redis service containers once integration tests need them.
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # TODO: install dependencies (e.g. `pip install -e ".[dev]"`)
      # TODO: run `pytest --cov=backend`
      - name: Placeholder
        run: echo "TODO -- add test steps"
