"""Application services: orchestrate repositories, agents, and the graph on behalf of API routes.

`audit.py` provides `log_audit_event`, a placeholder audit-logging call site
used by `backend/api/supervisor.py`.

TODO: implement domain services (e.g. TicketService, ConversationService) that
routes in backend/api depend on.
"""
