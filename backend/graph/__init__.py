"""LangGraph state graph package: wires agents together into a control flow.

TODO: define the graph state schema, nodes (per agent), conditional edges
(routing logic delegating to backend/policy), and checkpointing configuration.
"""
