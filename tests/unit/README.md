# tests/unit/

Fast, isolated unit tests. No network, database, or Redis dependencies —
mock/stub collaborators. Directory structure should mirror `backend/`.

## TODO

- [ ] Add unit tests as each `backend/` module gains an implementation.
