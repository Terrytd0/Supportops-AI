# tests/fixtures/

Shared test fixtures, factories, and sample data used across unit and
integration tests.

## TODO

- [ ] Add model factories (e.g. via `factory_boy`) once database models exist
- [ ] Add sample support-conversation fixtures for agent/graph testing
